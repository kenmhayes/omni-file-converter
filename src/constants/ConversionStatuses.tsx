// User friendly strings corresponding to conversion statues obtained from the GetSession API
const READABLE_CONVERSION_STATUSES: { [key: string]: string } = {
  NOT_STARTED: 'Not started',
  COMPLETED: 'Ready to download',
};

export default READABLE_CONVERSION_STATUSES;
